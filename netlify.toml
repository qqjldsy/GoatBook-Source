[build]
  command = "mkdocs build"
  publish = "site"

[build.environment]
  PYTHON_VERSION = "3.11"  # 与 GitHub Actions 一致
  PIP_CACHE_DIR = "~/.cache/pip"  # 加速依赖安装

[deploy]
  preview = true  # 开启 PR 预览部署