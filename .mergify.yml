pull_request_rules:
  - name: 🚀 自动合并 PR
    conditions:
      - base=main
      - "status-success=netlify/goatwiki/deploy-preview"
    actions:
      merge:
        method: merge

queue_rules: []